import { NavLink, useNavigate } from "react-router-dom";
import { 
  FaUniversity, 
  FaPowerOff, 
  FaChartBar, 
  FaRegAddressCard, 
  FaFileAlt, 
  FaExchangeAlt, 
  FaCreditCard, 
  FaCog 
} from "react-icons/fa";

function AdminSidebar() {
  const navigate = useNavigate();

  const handleLogout = () => {
    navigate('/');
  };

  // Shared classes for cleaner code and easy maintenance
  const linkBaseClass = "flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 text-slate-400 hover:bg-slate-800 hover:text-white group";
  const linkActiveClass = "bg-emerald-600 text-white shadow-md shadow-emerald-900/30 hover:bg-emerald-600";

  return (
    <div className="flex flex-col justify-between w-64 h-screen p-5 bg-slate-950 text-white border-r border-slate-900">
      <div>
        {/* Logo Section */}
        <div className="flex items-center gap-3 px-4 py-4 mb-8">
          <FaUniversity className="text-2xl text-emerald-500" />
          <span className="text-xl font-bold tracking-wide text-slate-100">NexaBank</span>
        </div>
        
        {/* Navigation Links */}
        <nav className="flex flex-col gap-1.5">
          <NavLink 
            to="/dashboard" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaChartBar className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Dashboard</span>
          </NavLink>
          
          <NavLink 
            to="/accounts" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaRegAddressCard className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Accounts</span>
          </NavLink>
          
          <NavLink 
            to="/statement" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaFileAlt className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Statement</span>
          </NavLink>
          
          <NavLink 
            to="/transfer" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaExchangeAlt className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Transfer</span>
          </NavLink>
          
          <NavLink 
            to="/cards" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaCreditCard className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Cards</span>
          </NavLink>
          
          <NavLink 
            to="/settings" 
            className={({ isActive }) => `${linkBaseClass} ${isActive ? linkActiveClass : ''}`}
          >
            <FaCog className="text-lg transition-transform group-hover:scale-105" />
            <span className="font-medium text-sm">Settings</span>
          </NavLink>
        </nav>
      </div>
      
      {/* Logout Action */}
      <button 
        onClick={handleLogout}
        className="flex items-center gap-3 w-full px-4 py-3 text-slate-400 hover:text-rose-400 rounded-xl hover:bg-rose-950/20 transition-colors duration-200 mt-auto border border-transparent hover:border-rose-900/30"
      >
        <FaPowerOff className="text-lg" />
        <span className="font-medium text-sm">Logout</span>
      </button>
    </div>
  );
}

export default AdminSidebar;
